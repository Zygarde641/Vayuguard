# Backend Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY backend/package*.json ./

# dev deps included: docker-compose overrides CMD with `npm run dev` (nodemon)
RUN npm install

COPY backend/ ./

EXPOSE 5000

CMD ["npm", "start"]
